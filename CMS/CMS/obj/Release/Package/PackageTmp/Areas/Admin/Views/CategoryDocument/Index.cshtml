
@{
    ViewBag.Title = "Quản lý Danh mục Tài liệu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div ng-controller="categoryDocumentController">
    <!--Header Section-->
    <div class="section-header">
        <ol class="breadcrumb">
            <li class="active text-uppercase">@ViewBag.Title</li>
        </ol>
    </div>

    <!--Body-->
    <div class="section-body">
        @*<input type="hidden" value="@WebSecurity.CurrentUserName" id="user" />*@
        <div class="row">
        </div>
        <hr class="ruler-lg" />
        <div class="card">
            <div class="card-body">
                @*<span us-spinner="{radius:15, width:4, length: 8}"></span>*@
                <!--Category Post-->
                <div class="col-md-12 col-sm-12 col-xs-12" id="categoryDocument">
                    <div id="gridContainer" dx-data-grid="gridCategoryDocuments"></div>
                    <div dx-context-menu="contextMenuCategoryDocument"></div>
                </div>

                <!--Popup Delete-->
                <div class="popup" dx-popup="popupDeleteCategoryDocument">
                    <div data-options="dxTemplate: { name:'templateDeleteCategoryDocument' }">
                        <div style="text-align: center" ng-cloak>{{titleDeleteCategoryDocument}}</div>
                        <br />
                        <div dx-button="{ text: 'Có', icon: 'floppy', onClick: ConfirmDeleteCategoryDocument, type: 'success' }"></div>
                        <div dx-button="{ text: 'Không', icon: 'clear', onClick: CancelDeleteCategoryDocument, type: 'danger' }"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




