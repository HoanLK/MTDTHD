
@{
    ViewBag.Title = "Quản lý Tài liệu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div ng-controller="documentController">
    <!--Header Section-->
    <div class="section-header">
        <ol class="breadcrumb">
            <li class="active text-uppercase">@ViewBag.Title</li>
        </ol>
    </div>

    <!--Body-->
    <div class="section-body">
        @*<input type="hidden" value="@WebSecurity.CurrentUserName" id="user" />*@
        <div class="row">

        </div>
        <hr class="ruler-lg" />

        <div class="card">
            <div class="card-body">
                @*<span us-spinner="{radius:15, width:4, length: 8}"></span>*@
                <!--Category Document-->
                <div class="col-md-12 col-sm-12 col-xs-12" id="document">
                    <div id="gridContainer" dx-data-grid="gridDocuments"></div>
                    <div dx-context-menu="contextMenuDocument"></div>
                </div>

                <!--Popup Delete-->
                <div class="popup" dx-popup="popupDeleteDocument">
                    <div data-options="dxTemplate: { name:'templateDeleteDocument' }">
                        <div style="text-align: center" ng-cloak>{{titleDeleteDocument}}</div>
                        <br />
                        <div dx-button="{ text: 'Có', icon: 'floppy', onClick: ConfirmDeleteDocument, type: 'success' }"></div>
                        <div dx-button="{ text: 'Không', icon: 'clear', onClick: CancelDeleteDocument, type: 'danger' }"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>




